{
  "name": "llm-leaderboard-account",
  "version": "1.0.0",
  "description": "LLM Leaderboard Account Infrastructure and Application",
  "main": "lib/leaderboard-stack.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy",
    "synth": "cdk synth",
    "bootstrap": "cdk bootstrap",
    "build-frontend": "cd frontend && npm run build",
    "deploy-all": "npm run build && npm run build-frontend && cdk deploy"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "18.14.6",
    "jest": "^29.5.0",
    "ts-jest": "^29.0.5",
    "aws-cdk": "2.87.0",
    "typescript": "~4.9.5"
  },
  "dependencies": {
    "aws-cdk-lib": "2.87.0",
    "constructs": "^10.0.0"
  }
}